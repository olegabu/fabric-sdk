CRYPTO_CONFIG_DIR=../fabric-starter/crypto-config

fabric2-api.cli.container=cli.${PEER_NAME:peer0}.${ORG:org1}.${DOMAIN:example.com}

#TODO: environment is not passed to docker container
fabric.peer.command=docker exec ${fabric2-api.cli.container} peer